{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @provable/contracts build && pnpm --filter @provable/web build:next",
  "outputDirectory": "apps/web/.next"
}
